/**
 * En esta demo el seed corre en el navegador (src/lib/seed-demo-data.ts)
 * la primera vez que abres la app, sobre localStorage.
 * No usa Firebase de la cliente.
 */
console.log(
  'Seed de consultorio: abre la app (npm run dev). Se insertan 8 pacientes y 8 citas inventadas en localStorage.',
)
